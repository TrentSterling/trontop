# Trontop visual inspiration

Saved from Trent's supplied generated concepts on 2026-09-05, unchanged.

- `processes-teal-concept.png`: teal/charcoal process view, crisp navigation icons,
  dense tree rows, inline resource history, clearly grouped inspector controls.
- `theme-directions-concept.png`: Porcelain, Carbon, Phosphor and Vector directions.
  These are reference names/styles, not implemented presets yet.
- `icon-lab-concept.png`: six identity directions and navigation/hardware/action
  icon vocabulary. Signal (A) is the first standalone candidate, not a final choice.
- `component-studies-concept.png`: compact controls, slim table rows, tidy numeric
  alignment and restrained grid/selection treatments in three theme directions.
- `signal-icon-candidate.png`: standalone Signal candidate generated using the
  built-in image tool from the supplied icon-lab board. Not wired into the EXE.

Use these for hierarchy, spacing, vector icon language, readable data and theme
exploration. Preserve Trontop's real telemetry and confirmation safeguards. Generated
values, app counts, paths and duplicated End Task buttons are not requirements or
runtime evidence. Do not bake the images into the application UI or copy their
text/icons as raster crops. The portable EXE must not depend on this directory.

Implementation direction: original code-drawn navigation/window icons, theme-derived
hover/selection surfaces, compact summary sparklines, stable aligned rows and a
readable inspector. Real executable icons need a bounded background Windows icon
cache; do not query the shell from the render thread.

## Signal generation prompt (built-in tool, 2026-09-05)

Use case: background-extraction / logo-brand. Edit the supplied Trontop icon lab
board into ONE standalone app icon asset: retain only the top-left A SIGNAL icon,
enlarge and faithfully reconstruct it cleanly. Preserve its rounded square teal
glass body, strong cream-white capital T with a separate purple horizontal cap above
it, front-on orientation and original proportions. This is a Windows task manager
app icon, so keep bold shapes and strong silhouette legible at small size. Output a
single centered icon with around 6% transparent padding, on a genuinely transparent
RGBA background. Remove the whole board, all labels, all other icons, atmosphere,
scenery, shadows outside the silhouette and surrounding glow. No text other than
the T shape. Do not render a checkerboard, floor, background color, duplicate small
thumbnail or mockup. The only visible object must be the single rounded-square
icon; transparent pixels outside it.
